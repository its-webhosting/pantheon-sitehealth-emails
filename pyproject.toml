[project]
name = "pantheon-sitehealth-emails"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "rich",
    "mysqlclient",
    "SQLAlchemy",
    "matplotlib",
    "boto3",
    "botocore",
    "Jinja2",
    "dnspython",
    "cloudflare",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []

